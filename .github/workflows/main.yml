name: Atualizar Dados FutStats

on:
  schedule:
    # Roda a cada 2 minutos para manter o Ao Vivo e a IA sempre atualizados
    - cron: '*/2 * * * *'
  workflow_dispatch: # Permite rodar manualmente clicando em "Run Workflow"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' # Versão estável para as novas bibliotecas do Google

      - name: Instalar Dependências
        run: |
          python -m pip install --upgrade pip
          # Instalando as bibliotecas necessárias, incluindo a nova google-genai
          pip install requests supabase google-genai

      - name: 📡 Executar Crawler Ao Vivo
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python crawler_ao_vivo.py

      - name: 📊 Executar Crawler de Tabelas
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python crawler.py

      - name: 🤖 Gerar Análise com IA
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python analise_ia.py
